# Models Module

Contains training functions for different NLP models:

- `bow_model.py`: Bag-of-Words + Naive Bayes.
- `logistic_model.py`: TF-IDF + Logistic Regression.
- Future: `roberta_model.py` for transformer-based models.