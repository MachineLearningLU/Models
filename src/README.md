# Source Code

Reusable Python modules for data loading, preprocessing, modeling, and utilities.

- `data/`: Data loading and preprocessing functions.
- `models/`: Model training functions (BOW, Logistic, etc.).
- `utils/`: Metrics, I/O helpers.