# Experiments

Runnable scripts to train and evaluate models. Each script loads data, trains, evaluates, and saves results to `results/`.

- `run_bow.py`: Bag-of-Words experiment.
- `run_logistic.py`: Logistic Regression experiment.