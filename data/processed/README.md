# Processed Data

Contains cleaned, preprocessed, and split datasets for experiments.

- Files here are generated from `data/raw/` via scripts in `scripts/`.
- Includes train/test splits and sampled subsets.