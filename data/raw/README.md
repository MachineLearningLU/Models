# Raw Data

Contains original datasets as downloaded or provided. Do not modify files here—use `data/processed/` for cleaned versions.

- `sentiment_data.csv`: Main dataset with ~241k rows of text and sentiment labels.
- `datasets/`: Zipped additional datasets from Kaggle. Unpack with `python scripts/unpack_datasets.py`.