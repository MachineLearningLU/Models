# Data Directory

This directory contains datasets used for training and evaluation.

- `raw/`: Raw, unprocessed datasets (e.g., downloaded CSVs, zipped files).
- `processed/`: Cleaned and split datasets ready for experiments.